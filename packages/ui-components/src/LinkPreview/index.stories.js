import React from 'react';
import LinkPreview from '.';

export default {
  title: 'Components/LinkPreview',
  component: LinkPreview,
};

const Template = (args) => <LinkPreview {...args} />;

export const Default = Template.bind({});

Default.args = {};
