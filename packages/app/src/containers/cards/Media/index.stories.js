import React from 'react';
import Media from './index';

export default {
  title: 'Containers/Cards/Media',
  component: Media,
};

const Template = (args) => <Media {...args} />;

export const Default = Template.bind({});
Default.args = {};
