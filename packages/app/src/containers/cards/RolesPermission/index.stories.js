import React from 'react';
import ScreenSaver from './index';

export default {
  title: 'Containers/Cards/Roles and Permissions',
  component: ScreenSaver,
};

const Template = (args) => <ScreenSaver {...args} />;

export const Default = Template.bind({});
Default.args = {};
