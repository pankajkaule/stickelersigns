import React from 'react';
import ProjectCreation from '.';

export default {
  title: 'Containers/ProjectCreation',
  component: ProjectCreation,
};

const Template = (args) => <ProjectCreation {...args} />;

export const Default = Template.bind({});

Default.args = {};
